<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hero Section</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

    .hero {
    width: 80%; /* Adds space from both left and right */
    height: 50vh; /* Adjust height to leave space from the top and bottom */
    background: url('/images/homepic.jpg') no-repeat center center/cover;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    padding: 80px 20px; /* Adds space from the top and bottom */
    position: relative;
    margin: 50px auto; /* Creates spacing from the header and bottom */
    border-radius: 10px; /* Optional: Adds rounded corners for a modern look */
}



        .hero::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5); /* Dark overlay for better readability */
        }

        .hero-content {
            position: relative;
            max-width: 80%;
            font-size: 2rem;
            font-weight: bold;
            line-height: 1.5;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-content {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .hero-content {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>

    <section class="hero">
        <div class="hero-content">
            "Every story has a voice. Every thought has a place. Welcome to Blogify, where words create wonders!"
        </div>
    </section>

</body>
</html>
